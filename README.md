# Dotnet Example

## How to run?

First, run the following commands to run API:

```bash
cd dotnetapi
dotnet run
```

Then, run the following command to run HTTP requests:

```
cd dotnethttp
dotnet run
```

# License

MIT License
